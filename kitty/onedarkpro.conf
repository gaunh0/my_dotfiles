# Primary
foreground              #abb2bf
background              #1e2229
selection_foreground    #abb2bf
selection_background    #3e4451

# Cursor
cursor                  #abb2bf
cursor_text_color       #282c34

# Vi mode cursor
vi_mode_cursor          #61afef
vi_mode_cursor_text_color #282c34

# URL hints
url_color               #abb2bf
url_style               underline

# Line number margin (if used)
# Kitty không có line_indicator như Alacritty, bỏ qua

# Footer bar – không áp dụng trực tiếp trong Kitty

# Search matches
# Kitty dùng highlight mặc định, có thể không tùy chỉnh riêng như Alacritty
# Tuy nhiên, bạn có thể dùng `highlight_fg` và `highlight_bg` nếu cần

# Normal colors
color0  #282c34
color1  #e06c75
color2  #98c379
color3  #e5c07b
color4  #61afef
color5  #c678dd
color6  #56b6c2
color7  #abb2bf

# Bright colors
color8  #5c6370
color9  #e06c75
color10 #98c379
color11 #e5c07b
color12 #61afef
color13 #c678dd
color14 #56b6c2
color15 #c8ccd4

# Dim colors (Kitty hỗ trợ dim qua `dim_*` nếu bật `dim_inactive_fonts`)
# Nhưng không có cấu hình `colorX_dim` riêng → bạn có thể bật `dim_inactive_windows yes`
# và dùng font hỗ trợ dim, hoặc thiết lập thủ công nếu cần (ít phổ biến)

# Optional: nếu muốn bật dim màu
# dim_fg                 #5c6370
# dim_bg                 #1e2229
